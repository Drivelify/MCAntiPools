package com.poolsync.mcantipools.pool;

/**
 * Represents the status of a connection pool
 */
public enum PoolStatus {
    // This source code is not being released publicly.
    
    HEALTHY,
    DEGRADED,
    OVERLOADED,
    OFFLINE
}
